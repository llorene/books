<h1>New Book</h1>

<%# <%= render 'form', book: @book %>
<%= simple_form_for [@author, @book ] do |f| %>
 <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :resume %>
    <%= f.input :pages %>
  </div>
  <%= f.submit "Submit Book", class: "btn btn-primary" %>
<% end %>

<%= link_to 'Back', authors_path %>
